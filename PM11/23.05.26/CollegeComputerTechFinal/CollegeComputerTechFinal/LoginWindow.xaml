<Window x:Class="CollegeComputerTechFinal.LoginWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Авторизация" Height="300" Width="400" 
        WindowStartupLocation="CenterScreen" ResizeMode="NoResize">

    <Grid Margin="20">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="80"/>
            <ColumnDefinition Width="*"/>
        </Grid.ColumnDefinitions>

        <TextBlock Grid.Row="0" Grid.Column="0" Text="Логин:" VerticalAlignment="Center" FontSize="14"/>
        <TextBox x:Name="txtLogin" Grid.Row="0" Grid.Column="1" Margin="5" FontSize="14"/>

        <TextBlock Grid.Row="1" Grid.Column="0" Text="Пароль:" VerticalAlignment="Center" FontSize="14"/>
        <PasswordBox x:Name="txtPassword" Grid.Row="1" Grid.Column="1" Margin="5" FontSize="14"/>

        <TextBlock x:Name="txtError" Grid.Row="2" Grid.ColumnSpan="2" Foreground="Red" TextAlignment="Center" Margin="5"/>

        <Button x:Name="btnLogin" Grid.Row="3" Grid.ColumnSpan="2" Content="Войти" Width="100" Height="30" 
                HorizontalAlignment="Center" Margin="10" Click="BtnLogin_Click" FontSize="14"/>
    </Grid>
</Window>